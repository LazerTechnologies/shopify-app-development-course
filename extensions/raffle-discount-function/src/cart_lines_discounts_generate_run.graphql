query CartInput {
  cart {
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
    }
    raffleDiscountAmount: attribute(key: "raffle_discount_amount") {
      value
    }
  }
  discount {
    discountClasses
  }
}
